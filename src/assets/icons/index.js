const requireAll = requireContext => requireContext.keys().map(requireContext)
const req = require.context('./svg/matters', false, /\.svg$/)
const req2 = require.context('./svg/', false, /\.svg$/)
requireAll(req)
requireAll(req2)
